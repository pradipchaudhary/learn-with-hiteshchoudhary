import React from "react";
import Layout from "../Layout";
import BlogCom from "../Blog";

const Blog = () => {
    return (
        <Layout>
            <BlogCom />
        </Layout>
    );
};

export default Blog;
